const isLoop = false;

while (isLoop) {
  console.log('This loop will never run');
}

do {
  console.log('It will run at least once');
} while (isLoop);

// condition false holew at least ekbar cholbe
